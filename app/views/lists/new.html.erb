<%= simple_form_for [ @list ] do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Create Watch List", class: "btn btn-primary" %>
<% end %>
<br>
<%= link_to "Back to My Lists", lists_path %>
